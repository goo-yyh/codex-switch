# 仓库结构整理与回归验证（2026-09-17）

基于 `84bc533` 整理；功能约束见[架构说明](../development/architecture.md)。本文记录本次重构的提交前验证结果。

## 修改范围

- 桌面入口从 902 行拆至 252 行：编辑草稿、设置、IPC 契约、预览模拟和状态控制各自独立；复用配置命名及凭据作用域提示逻辑。
- 原生入口从 456 行拆至 81 行：命令、状态、服务协调和托盘分别维护。宏注册保持原有命令名称。
- 网关由单文件拆为转发、历史、HTTP 限制、探测和回归测试；对外 `gateway::*` 路径保持兼容。展开流处理宏，注释请求计数与缓存捕获的生命周期。
- 删除无实际调用的旧逐连接批量创建、容错加载、选择即启用分支及其专属测试；保留 SQLite 迁移、历史路由读取、配置文件恢复和协议回归。
- 删除 UI 未消费的 `needsApply` / `unavailable` 字段、废弃预览命令、不可达的编辑锁定弹窗和恢复按钮；全局锁定蒙层及后台校验仍保留。
- 凭据库写入后报错时也清理新凭据；操作失败后刷新的旧托盘反馈不再覆盖原错误。两处都有新增回归断言。
- 中英文下载页复用发布条件与卡片组件。`pnpm test` 纳入原生协调测试；README 和中英文开发文档增加模块职责说明。

## 自动检查

| 验证 | 结果与边界 |
| --- | --- |
| TypeScript + Astro 检查 | 通过；Astro 17 个文件，0 错误、警告、提示 |
| 桌面 UI | 63 项通过，使用模拟 IPC |
| Rust 核心 | 73 项通过，包含配置恢复、数据库事务和凭据补偿 |
| 摘要测试工具的离线单元测试 | 2 项通过，未执行真实供应商调用 |
| 跨模块协议集成 | 18 项通过，使用临时文件和 loopback 合成服务 |
| 原生协调 | 3 项通过 |
| 固定上游模块 | 190 项通过；来源校验通过，未修改上游代码 |
| Rust 格式与 Clippy | core、desktop 全目标通过，warnings 视为错误；包含 tray-smoke 编译 |
| 桌面前端与网站构建 | Node 22，完整构建通过；38 个静态页面 |
| 网站链接和 SEO | 38 页通过，中英文配对、锚点、元数据及 sitemap 检查通过 |
| 部署与双语 404 | 24 项通过，使用本地静态服务模拟路由 |
| 凭据扫描与 diff 空白检查 | 通过 |

网站构建使用 `PUBLIC_SITE_URL=https://codex-switch.test`、`PUBLIC_SITE_INDEXABLE=false`，不是生产发布。旧接口删除时移除了仅调用这些接口的测试；当前配置流程的凭据范围、默认模型、完整恢复、事务回滚和历史别名测试继续保留。

## 实际交互回归

浏览器访问本地 Vite 预览，使用虚构 Key，完成新增智谱配置并勾选两个模型、保存、勾选配置、开启和关闭。保存和勾选都未自动启动；启用后只有关闭服务操作；Esc 无法关闭锁定蒙层；关闭后选择保留且设置重新可用。设置页可保存远程压缩开关，布局正常。

重新编译并启动 `tray-smoke` 隔离程序，使用内存数据库、临时配置和 A/B/C 示例：连续勾选 A、B 后菜单未关闭；取消 A 并重新打开菜单，只有 B 被选中，状态仍为“Codex Switch 已关闭”。之后通过“退出应用”关闭测试程序。

## 未覆盖范围

未启动真实 Codex 客户端，未读取真实模型密钥发请求，未验证 Windows 实机菜单、安装包、签名或 Vercel 线上部署。浏览器模拟不会跨页面刷新持久化，真实设置持久化由现有原生数据库路径负责。
