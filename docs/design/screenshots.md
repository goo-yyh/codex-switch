# 文档界面截图

采集日期：2026-09-17。界面代码基线：`21fc74fa2bb422fb0960aec709ff3e1244ebd02b`。

截图来自 `pnpm dev` 启动的真实 React 应用浏览器预览，通过实际点击、填写、保存和切换操作采集。没有重新绘制或修改 DOM 来伪造界面。使用默认 1280 × 720 视口，首页保留完整画面，表单与弹窗以系统图片工具裁剪到内容区域；未后期替换文字或按钮。

- 「Kimi 示例」：`kimi-k3`、`kimi-k2.7-code`，默认 `kimi-k3`。
- 「DeepSeek 示例」：`deepseek-v4-flash`。
- Key 为明确虚构的占位字符串，保持密码遮罩。
- 浏览器预览只保留页内数据；连接、备份、应用检测和进程状态是原有预览实现的模拟值。
- 未读取真实 Key、调用服务、打开原生应用、启动或重启 Codex，也未改写真实配置。

| 文件（`apps/website/public/screenshots/`） | 内容                                      |
| ------------------------------------------ | ----------------------------------------- |
| `connection.png`                           | 新增配置的服务、套餐、名称、地址与接口    |
| `models.png`                               | 滚动后的密钥、模型选择与保存操作          |
| `model-options.png`                        | 逐模型设置弹窗                            |
| `selected.png`                             | 首页勾选两个配置，尚未开启                |
| `enabled.png`                              | 首页开启状态与打开 Codex 入口（模拟连接） |
| `settings.png`                             | 远程压缩、登录启动、检测与恢复入口        |

除弹窗截图外保留页面顶部预览提示。README、教程和首页另加说明，避免将界面展示当作原生功能或接口实测证据。

重新采集时从空白浏览器预览开始，用相同示例完成上述操作；若 UI 改变，同步更新教程措辞。运行 `pnpm build`、`pnpm check:links` 并检查桌面与窄屏布局、图片加载和说明文字。
