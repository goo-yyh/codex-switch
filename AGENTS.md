# 模型预设维护

## 远程模型目录版本

- `packages/provider-registry/version.json` 是数据版本的唯一来源，桌面内置目录与网站 `/registry/models-v1.json` 必须共用此文件，不维护两份版本或模型数据副本。初始 `version` 为 `1`，使用正整数（不超过 `4294967295`）。
- 修改公开目录 `models.json` 或 `providers.json` 后，发布前必须将 `version` 递增，并在同一次提交中包含数据和版本变更，再部署网站；同一批修改只需递增一次。仅修改界面、文档或同步实现不递增目录版本；应用版本独立管理。
- 已发布的数据版本不可复用或降低。修复已发布的错误数据也必须发布更高版本；相同版本下改数据不会触发同步。
- `schemaVersion` 仅表示 JSON 格式兼容性，目前为 `1`；格式不兼容的变化需同步适配客户端。`revision` 是网站构建自动生成的内容哈希，仅用于追踪，不能代替 `version` 判断是否更新。
- 桌面每次启动后台检查一次。远程 `version` 相同或更低时跳过合并和缓存写入，首次两端均为 `1` 也如此；只有更高版本才合并成功后保存新版本。格式错误、缺少版本或请求/保存失败时保留本地目录和版本。
- 更新只补充已有厂商、对应普通服务或套餐范围内的新模型及默认能力；保留本地已有参数、用户配置、密钥、默认模型和勾选状态，不自动启用服务，不接受远程修改请求地址。普通候选和套餐候选分别遵守最多 5 个的限制。
- 版本相关修改须验证首次同版本跳过、较高版本合并、较低版本不回退、缓存重开与失败回退，并执行网站构建和 `pnpm check:deployment`，确认生成 JSON 与共享版本文件一致。测试使用相对内置版本，避免正常递增版本导致测试失效；不为这些检查调用真实模型 API。

## 候选数量与排序

- 根据用户要求，每个厂商的内置候选最多保留 5 个，按已核对的版本顺序从新到旧取前 5 个；不足 5 个则保留全部，不为凑数加入旧型号或别名。
- 先排除已知失败、下线或无效的型号，再按版本精简。同版本变体维持现有相邻顺序，不把参数规模当成版本号。
- 2026-09-17 再次精简后：千问 5 个、MiniMax 5 个、智谱 GLM 5 个、Kimi 4 个、DeepSeek 4 个，共 23 个。
- 2026-09-20 数据版本 2：千问新增 `qwen3.8-omni-flash`，智谱新增 `glm-5.3-flashx`，DeepSeek 移出两个旧兼容别名；普通候选为 5 / 5 / 5 / 4 / 2，共 21 个。套餐范围不变，保留移出候选的历史能力元数据。
- 数量限制针对内置候选；用户手填模型和已有配置仍可保留。套餐专用列表独立维护，也不超过 5 个。

## 千问 Responses 排除名单

根据用户要求，2026-09-16 从 `packages/provider-registry/models.json` 的千问候选列表中移除以下 8 个实测失败的模型，先保留实测返回完整文本的 35 个，当日先精简为最新 10 个，2026-09-17 再按最新要求保留前 5 个。默认模型仍为 `qwen3.8-max`。

测试地址：`https://dashscope.aliyuncs.com/compatible-mode/v1/responses`。

| 模型                            | 实测与复查结果                        |
| ------------------------------- | ------------------------------------- |
| `qwen3.6-27b`                   | HTTP 400，`Unsupported model`         |
| `qwen3-235b-a22b-instruct-2507` | HTTP 400，当前模型未开启 Agent 能力   |
| `qwen3-14b`                     | HTTP 500，上游 Event 响应结构校验失败 |
| `qwen-max`                      | HTTP 500，上游 Event 响应结构校验失败 |
| `qwen-coder-plus`               | HTTP 500，上游 Event 响应结构校验失败 |
| `qwen-coder-turbo`              | HTTP 500，上游 Event 响应结构校验失败 |
| `qwq-plus`                      | HTTP 500，上游 Event 响应结构校验失败 |
| `qwen-long`                     | HTTP 500，上游 Event 响应结构校验失败 |

依据：[逐模型实测报告](docs/testing/provider-responses-models-2026-09-16.md) 及其中链接的首轮、复查脱敏记录。HTTP 500 表示本次上游实现异常，不能表述成永久不支持 Responses；也不能推断这些模型的 Chat Completions 已通过。

- 更新官方模型目录或排序时，不要将这些 ID 自动重新加入预设。重新加入前，需要同一目标地址上该模型的成功实测证据，并同步更新本文件与模型目录说明。
- 真实 API 测试只在用户明确要求测试时运行；普通列表编辑、构建和本地测试不主动读取 `.env` 发请求。
- 文本连通须检查成功 HTTP 状态、`status=completed` 和非空输出，不能只看 HTTP 200。严格文本校验、SSE、工具调用、图片和远程压缩须分别说明验证范围。
- 此次移除针对内置候选，不批量改写用户已有配置或自定义模型；保留历史报告和脱敏证据中的原始模型列表与结果。
