---
import DownloadDropdown from './DownloadDropdown';
interface Props {
  product?: 'switch' | 'codex';
  lang?: 'zh' | 'en';
}
const { product = 'switch', lang = 'zh' } = Astro.props;
---

<DownloadDropdown product={product} lang={lang} client:load />
